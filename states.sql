
# ========================================================================================

create table states
(
    id         bigint unsigned auto_increment primary key,
    # name       varchar(255)
    # value, etc.
)
    collate = utf8mb4_unicode_ci;

# ========================================================================================
